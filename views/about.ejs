<%- include ('header') %> 
<h1>Hai Welcome This is About page.</h1>

<p> 'Scriptlet' tag, for control-flow, no output
     ‘Whitespace Slurping’ Scriptlet tag, strips all whitespace before it
    Outputs the value into the template (HTML escaped)
     Outputs the unescaped value into the template
     Comment tag, no execution, no output
     Outputs a literal 
     Plain ending tag
     Trim-mode ('newline slurp') tag, trims following newline
     ‘Whitespace Slurping’ ending tag, removes all whitespace after it
    </p> 
<%- include ('footer') %> 